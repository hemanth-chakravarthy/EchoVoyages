// This file is used to set up the Jest environment for ES modules
// It's referenced in jest.config.js

// Set up global variables or configurations here if needed

// Note: In ES modules, we'll handle mocking in individual test files
// rather than globally to avoid issues with ES module imports
